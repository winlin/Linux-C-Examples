>>>>>>>>>>
算法
度量算法的两种方法：
   事后统计法
   事前分析法

时间复杂度：
   T(n)=O(f(n))
空间复杂度:
   S(n)=O(f(n))

>>>>>>>>>>
敏捷开发
测试先行

>>>>>>>>>
对于一个类来说，我们只需要重载operator<即可，
我们可以使用operator<号，来获得其他条件的结果
a1<a2;
a1>a2    a2<a1
a1==a2   !(a1<a2)&&!(a2<a1) 
....
一般的通用算法只要求其操作的类重载operator<即可。

>>>>>>>>>>>
常用的经典算法

冒泡排序
       在for循环里面最好不要出现if语句，因为如果for循环没有if语句，编译器就会对其汇编代码进行优化，
但是一旦有if语句，编译器可能就不会进行优化，从而导致不能提高程序的效率。
选择排序
       实际上，选择排序的效率要比冒泡稍微高一点，虽然两者都是O(n~2)，主要是选择排序中没有大量的数值交换。
插入排序
	
快速排序