>>>>>>>>>>>>>>>
模板 template
     类的模板不可以重载

template<typename T>
class ClassName{};

>>>>>>>>>>     
函数模板
    template<typename T>
    T func(T a){}
    模板形参表不可为空
    函数模板虽然支持常规参数，但是可以写成函数参数，所以不要使用常规参数
    函数模板常规参数不可有默认值；
    函数模板参数不可有默认值；
    函数模板具有参数“类型推断”的功能；
    模板特殊化的时候，不会继承任何代码，必须全部重写类的实现。


>>>>>>>>>>>>
标准库

注意，标准库中的各种容器出于效率问题都不会进行访问越界检查，
也不会抛出异常等。这个要注意。

>>>>>>>>>>>>
迭代器存有它们所指向的容器的状态信息，即迭代器对每种类型的容器都有一个实现；
容器类基本都提供成员函数begin和end，begin函数返回一个指向容器中第一个元素，
end返回一个指向容器中最后一个元素的下一个位置 “[  )”
>>>>>>>>>>>>>>>>
标准容器的共性
vector deque list set/map ....
1.构造函数：无参构造，拷贝构造，区间构造(两个迭代器表示的两个位置)
2.析构函数
3.迭代器相关：正向.begin(),end()；反向rbegin(),rend()
4.迭代器支持多种运算符
5.多种类型的迭代器: const_iterator reverse_iterator...