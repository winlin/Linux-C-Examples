	
	1.dms  --------- 数据采集系统 + 数据整合系统

	UNIX下会把用户登录，登出的信息全部记录下来
	wtmpx 二进制文件：
	      /usr/adm/wtmpx
	      
	有专门的客户端程序，读取用户信息，然后定时发送到服务器.
	
	分析wtmpx文件的结构：
		注意：最后一条保存IP地址的长度实际有效数据是 257，但是由于对齐，后面还有一个'空'字节。
		char ip[257];

================================================================================
	
	1.清空一个文件
		cat /dev/null > filename